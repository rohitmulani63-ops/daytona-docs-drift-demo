[project]
name = "docs-drift-demo"
version = "0.1.0"
description = "Small CLI used to demonstrate docs-drift repair workflows."
readme = "README.md"
requires-python = ">=3.10"
license = {text = "MIT"}
authors = [{name = "Rohit Mulani"}]

[project.scripts]
docs-drift-demo = "docs_drift_demo.cli:main"

[tool.pytest.ini_options]
pythonpath = ["src"]